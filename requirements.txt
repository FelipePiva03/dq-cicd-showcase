# Local dev requirements — mirrors [project.dependencies] +
# [project.optional-dependencies] dev,spark in pyproject.toml.
#
# Use this when you don't want editable-install (`pip install -e .[dev,spark]`).
# CI installs via pyproject extras; this file is for local convenience.

# --- runtime ---
great-expectations==1.5.6
delta-spark==3.2.0
pyyaml>=6.0

# --- dev ---
pytest>=8.0
pytest-cov>=5.0
chispa>=0.10.1
ruff==0.6.9

# --- spark (local testing) ---
pyspark==3.5.1
